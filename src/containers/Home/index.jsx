import React from 'react';
import Tabs from '../../components/Tabs';
import { HomeWrapper } from './style';

const Home = (props) => {
  return (
    <HomeWrapper>
      <Tabs />
    </HomeWrapper>
  );
};

export default Home;
